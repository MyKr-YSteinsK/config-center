demo:
  baseUrl: http://localhost:8080
  app: demo-app
  env: dev
  featureName: new-checkout
  userId: u3000
  http:
    connectTimeoutMs: 800
    readTimeoutMs: 3000
  watch:
    enabled: true
    timeoutSeconds: 10
    readTimeoutMarginMs: 2000
    rounds: 5
